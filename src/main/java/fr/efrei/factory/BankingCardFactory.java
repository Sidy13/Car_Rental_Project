package fr.efrei.factory;

import fr.efrei.domain.BankingCard;
import fr.efrei.util.Helper;

public class BankingCardFactory {
//    public static BankingCard createBankingCard(String number, String expirationDate, String cvv) {
//        if (Helper.isNullOrEmpty(number) || Helper.isNullOrEmpty(expirationDate) || Helper.isNullOrEmpty(cvv)) {
//            return null; // Les champs ne peuvent pas être vides
//        }
//        if (!Helper.validateCardDetails(number, expirationDate, cvv)) {
//            return null;
//        }
//        return new BankingCard(number, expirationDate, cvv);
//    }
}